import React from "react";
import './PlusButton.css';

const PlusButton = () => {

    const addButtonOpen = () => {

    }

    return <div className="plus-button" onClick={addButtonOpen}>
        +
    </div>
}

export default PlusButton;